apiVersion: v1
kind: Service
metadata:
  name: logistics-svc
  namespace: ultra-sync
  labels:
    app: logistics
spec:
  selector:
    app: logistics
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
