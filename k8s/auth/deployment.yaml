apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  namespace: ultra-sync
  labels:
    app: auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: auth
          image: ghcr.io/chanon/ultra-sync/auth:latest
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: AUTH_DB_DSN
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: db-dsn
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: redis-url
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger.ultra-sync.svc.cluster.local:4318"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
